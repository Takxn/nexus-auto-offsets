# nexus-auto-offsets
IDA Pro 9.x plugin: Auto-generates Offsets.h for Rust (IL2CPP) from GameAssembly.dll. Scans TypeInfos, extracts BaseNetworkable decrypt chain &amp; crypto ops, outputs full C++ header. Includes live offset tester via NullKD kernel driver. Use with Il2CppDumper.
